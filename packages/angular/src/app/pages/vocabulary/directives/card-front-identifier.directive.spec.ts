import { CardFrontIdentifierDirective } from './card-front-identifier.directive';

describe('CardFrontIdentifierDirective', () => {
  it('should create an instance', () => {
    const directive = new CardFrontIdentifierDirective();
    expect(directive).toBeTruthy();
  });
});

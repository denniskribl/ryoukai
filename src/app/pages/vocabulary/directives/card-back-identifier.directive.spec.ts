import { CardBackIdentifierDirective } from './card-back-identifier.directive';

describe('CardBackIdentifierDirective', () => {
  it('should create an instance', () => {
    const directive = new CardBackIdentifierDirective();
    expect(directive).toBeTruthy();
  });
});
